<main class="main">
  <div class="inner">
      <div class="greeting"> 
      <% if user_signed_in? %>
        <%= link_to "こんにちは#{current_user.name}さん", "/users/#{current_user.id}", class: :greeting__link%> 
        <% end %>
      </div>

      <div class="card__wrapper">
      
        <%= render partial: 'prototype', collection: @prototypes %>
      </div>

 </div>
</main>


